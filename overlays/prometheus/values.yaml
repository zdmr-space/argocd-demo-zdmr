grafana:
  enabled: true
  adminPassword: prom-operator
  ingress:
    enabled: true
    hosts:
      - grafana.zdmr.arpa
prometheus:
  ingress:
    enabled: true
    hosts:
      - prometheus.zdmr.arpa